(set-info :smt-lib-version 2.6)
(set-logic QF_S)
(set-info :source |
Generated by: Hongjian Jiang, Oliver Markgraf, Daniel Stan, Anthony W. Lin, Philipp Ruemmer
Generated on: 2025-04-11
Application: Using String Solvers to solve RMC problems encoded as CHC over Strings
Target solver: cvc5, OSTRICH, Z3, Z3alpha, Z3-noodler
Time limit: 60.0
Benchmarks are extracted by running HornStr https://arg-git.informatik.uni-kl.de/pub/string-chc-lib on all benchmarks
provided in the repository and gathering the string queries sent to the string solvers.
|)
(declare-fun X () String)
(assert (let ((a!1 (re.* (re.union (re.union (str.to_re "2") (str.to_re "0"))
                           (str.to_re "1")))))
  (str.in_re X a!1)))
(assert (str.in_re X (re.++ (str.to_re "20") (re.* (str.to_re "0")))))
(push 1)
(assert (str.in_re X (str.to_re "")))
(set-info :status unknown)
(check-sat)
(pop 1)
(push 1)
(assert (str.in_re X
           (re.union (re.union (str.to_re "0") (str.to_re "2")) (str.to_re "1"))))
(set-info :status unknown)
(check-sat)
(pop 1)
(push 1)
(assert (let ((a!1 (re.++ (re.union (re.union (str.to_re "0") (str.to_re "2"))
                            (str.to_re "1"))
                  (re.union (re.union (str.to_re "0") (str.to_re "2"))
                            (str.to_re "1")))))
  (str.in_re X a!1)))
(set-info :status unknown)
(check-sat)
(assert (distinct X "20"))
(set-info :status unknown)
(check-sat)
(pop 1)
(push 1)
(assert (let ((a!1 (re.++ (re.union (re.union (str.to_re "0") (str.to_re "2"))
                            (str.to_re "1"))
                  (re.union (re.union (str.to_re "0") (str.to_re "2"))
                            (str.to_re "1")))))
(let ((a!2 (re.++ a!1
                  (re.union (re.union (str.to_re "0") (str.to_re "2"))
                            (str.to_re "1")))))
  (str.in_re X a!2))))
(set-info :status unknown)
(check-sat)
(assert (distinct X "200"))
(set-info :status unknown)
(check-sat)
(pop 1)
(push 1)
(assert (let ((a!1 (re.++ (re.union (re.union (str.to_re "0") (str.to_re "2"))
                            (str.to_re "1"))
                  (re.union (re.union (str.to_re "0") (str.to_re "2"))
                            (str.to_re "1")))))
(let ((a!2 (re.++ a!1
                  (re.union (re.union (str.to_re "0") (str.to_re "2"))
                            (str.to_re "1")))))
(let ((a!3 (re.++ a!2
                  (re.union (re.union (str.to_re "0") (str.to_re "2"))
                            (str.to_re "1")))))
  (str.in_re X a!3)))))
(set-info :status unknown)
(check-sat)
(assert (distinct X "2000"))
(set-info :status unknown)
(check-sat)
(pop 1)
(push 1)
(assert (let ((a!1 (re.++ (re.union (re.union (str.to_re "0") (str.to_re "2"))
                            (str.to_re "1"))
                  (re.union (re.union (str.to_re "0") (str.to_re "2"))
                            (str.to_re "1")))))
(let ((a!2 (re.++ a!1
                  (re.union (re.union (str.to_re "0") (str.to_re "2"))
                            (str.to_re "1")))))
(let ((a!3 (re.++ a!2
                  (re.union (re.union (str.to_re "0") (str.to_re "2"))
                            (str.to_re "1")))))
(let ((a!4 (re.++ a!3
                  (re.union (re.union (str.to_re "0") (str.to_re "2"))
                            (str.to_re "1")))))
  (str.in_re X a!4))))))
(set-info :status unknown)
(check-sat)
(assert (distinct X "20000"))
(set-info :status unknown)
(check-sat)
(pop 1)
