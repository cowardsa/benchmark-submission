(set-info :smt-lib-version 2.6)
(set-logic QF_S)
(set-info :source |
Generated by: Hongjian Jiang, Oliver Markgraf, Daniel Stan, Anthony W. Lin, Philipp Ruemmer
Generated on: 2025-04-11
Application: Using String Solvers to solve RMC problems encoded as CHC over Strings
Target solver: cvc5, OSTRICH, Z3, Z3alpha, Z3-noodler
Time limit: 60.0
Benchmarks are extracted by running HornStr https://arg-git.informatik.uni-kl.de/pub/string-chc-lib on all benchmarks
provided in the repository and gathering the string queries sent to the string solvers.
|)
(set-info :license "https://creativecommons.org/licenses/by/4.0/")
(set-info :category "industrial")
(declare-fun X () String)
(assert (let ((a!1 (re.union (re.union (re.union (str.to_re "m") (str.to_re "t"))
                               (str.to_re "n"))
                     (str.to_re "b"))))
  (str.in_re X (re.* a!1))))
(assert (let ((a!1 (re.++ (re.++ (re.* (str.to_re "n")) (str.to_re "b"))
                  (re.* (str.to_re "n")))))
  (and (str.in_re X a!1) true)))
(push 1)
(assert (str.in_re X (str.to_re "")))
(set-info :status unknown)
(check-sat)
(pop 1)
(push 1)
(assert (let ((a!1 (re.union (re.union (re.union (str.to_re "m") (str.to_re "t"))
                               (str.to_re "n"))
                     (str.to_re "b"))))
  (str.in_re X a!1)))
(set-info :status unknown)
(check-sat)
(assert (distinct X "b"))
(set-info :status unknown)
(check-sat)
(pop 1)
(push 1)
(assert (let ((a!1 (re.union (re.union (re.union (str.to_re "m") (str.to_re "t"))
                               (str.to_re "n"))
                     (str.to_re "b"))))
  (str.in_re X (re.++ a!1 a!1))))
(set-info :status unknown)
(check-sat)
(assert (distinct X "bn"))
(set-info :status unknown)
(check-sat)
(assert (distinct X "nb"))
(set-info :status unknown)
(check-sat)
(pop 1)
(check-sat)
(exit)
