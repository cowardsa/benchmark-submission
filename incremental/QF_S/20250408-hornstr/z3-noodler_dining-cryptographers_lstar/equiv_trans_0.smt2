(set-info :smt-lib-version 2.6)
(set-logic QF_S)
(set-info :source |
Generated by: Oliver Markgraf, Hongjian Jiang, Daniel Stan, Anthony W. Lin, Philipp Ruemmer
Generated on: 2025-04-08
Application: Using String Solvers to solve RMC problems encoded as CHC over Strings
Target solver: cvc5, OSTRICH, Z3, Z3alpha, Z3-noodler
Time limit: 60.0
Benchmarks are extracted by running HornStr https://arg-git.informatik.uni-kl.de/pub/string-chc-lib on all benchmarks
provided in the repository and gathering the string queries sent to the string solvers.
|)
(set-info :license "https://creativecommons.org/licenses/by/4.0/")
(set-info :category "industrial")
(declare-fun varout () String)
(assert (let ((a!1 (re.union (re.union (re.union (str.to_re "1") (str.to_re "k"))
                               (str.to_re "0"))
                     (str.to_re "f"))))
(let ((a!2 (re.union (re.union (re.union a!1 (str.to_re "h")) (str.to_re "c"))
                     (str.to_re "i"))))
(let ((a!3 (re.union (re.union (re.union a!2 (str.to_re "g")) (str.to_re "e"))
                     (str.to_re "l"))))
(let ((a!4 (re.union (re.union (re.union a!3 (str.to_re "j")) (str.to_re "a"))
                     (str.to_re "d"))))
  (str.in_re varout (re.* (re.union a!4 (str.to_re "b")))))))))
(declare-fun varin () String)
(assert (let ((a!1 (re.union (re.union (re.union (str.to_re "1") (str.to_re "k"))
                               (str.to_re "0"))
                     (str.to_re "f"))))
(let ((a!2 (re.union (re.union (re.union a!1 (str.to_re "h")) (str.to_re "c"))
                     (str.to_re "i"))))
(let ((a!3 (re.union (re.union (re.union a!2 (str.to_re "g")) (str.to_re "e"))
                     (str.to_re "l"))))
(let ((a!4 (re.union (re.union (re.union a!3 (str.to_re "j")) (str.to_re "a"))
                     (str.to_re "d"))))
  (str.in_re varin (re.* (re.union a!4 (str.to_re "b")))))))))
(declare-fun X () String)
(declare-fun reg1 () String)
(declare-fun Y () String)
(declare-fun reg0 () String)
(declare-fun isin () Bool)
(declare-fun isout () Bool)
(assert (let ((a!1 (re.union (re.union (re.union (str.to_re "a") (str.to_re "b"))
                               (str.to_re "c"))
                     (str.to_re "d")))
      (a!4 (re.union (re.union (re.union (str.to_re "j") (str.to_re "k"))
                               (str.to_re "i"))
                     (str.to_re "l"))))
(let ((a!2 (re.union (re.union (re.union a!1 (str.to_re "e")) (str.to_re "f"))
                     (str.to_re "g"))))
(let ((a!3 (str.in_re reg0 (re.* (re.union a!2 (str.to_re "h"))))))
  (or (and true
           (= X (str.++ reg1))
           (= Y (str.++ reg1))
           a!3
           (str.in_re reg1 (re.* a!4))
           (= X varin)
           isin
           (= Y varout)
           isout))))))
(set-info :status unknown)
(check-sat)
(set-info :status unknown)
(check-sat)
(exit)
