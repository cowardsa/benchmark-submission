(set-info :smt-lib-version 2.6)
(set-logic QF_S)
(set-info :source |
Generated by: Oliver Markgraf, Hongjian Jiang, Daniel Stan, Anthony W. Lin, Philipp Ruemmer
Generated on: 2025-04-08
Application: Using String Solvers to solve RMC problems encoded as CHC over Strings
Target solver: cvc5, OSTRICH, Z3, Z3alpha, Z3-noodler
Time limit: 60.0
Benchmarks are extracted by running HornStr https://arg-git.informatik.uni-kl.de/pub/string-chc-lib on all benchmarks
provided in the repository and gathering the string queries sent to the string solvers.
|)
(set-info :license "https://creativecommons.org/licenses/by/4.0/")
(set-info :category "industrial")
(declare-fun X () String)
(assert (str.in_re X (re.* (re.union (str.to_re "N") (str.to_re "T")))))
(assert (let ((a!1 (re.union (str.to_re "")
                     (re.++ (str.to_re "N") (re.* (str.to_re "N"))))))
(let ((a!2 (str.in_re X (re.++ (re.* (str.to_re "N")) a!1))))
  (and a!2 true))))
(push 1)
(assert (str.in_re X (str.to_re "")))
(set-info :status unknown)
(check-sat)
(assert (distinct X ""))
(set-info :status unknown)
(check-sat)
(pop 1)
(push 1)
(assert (str.in_re X (re.union (str.to_re "N") (str.to_re "T"))))
(set-info :status unknown)
(check-sat)
(assert (distinct X "N"))
(set-info :status unknown)
(check-sat)
(pop 1)
(push 1)
(assert (str.in_re X
           (re.++ (re.union (str.to_re "N") (str.to_re "T"))
                  (re.union (str.to_re "N") (str.to_re "T")))))
(set-info :status unknown)
(check-sat)
(assert (distinct X "NN"))
(set-info :status unknown)
(check-sat)
(pop 1)
(exit)
